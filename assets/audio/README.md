slash.wav is an original synthesized blade swoosh, generated by
`java tools/GenerateSlashSound.java` from the project root (JDK 17+).
It plays when each slash becomes visible, including delayed multi-hit effects.
